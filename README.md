# Password Strength Checker

Python script to check password strength. Evaluates length, digits, upper/lowercase, and symbols.

## Usage

Run `python password_check.py` and follow the instructions.

## Requirements

- Python 3.x
